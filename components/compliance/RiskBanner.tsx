export function RiskBanner() {
  return (
    <div role="note" className="meta" style={{ borderBottom: '1px solid var(--rule)', padding: 'var(--space) 0' }}>
      <span className="dot">●</span> Illustrative scenarios only. Figures are estimates based on
      assumptions you control, are not a forecast, and are not a guarantee of any result. Not
      investment advice. Capital is at risk.
    </div>
  );
}
